/**
 * 🎮 模拟页
 *
 * 使用 SimulateTemplate 模板渲染
 * 可通过 config props 自定义资产、模拟游戏列表、PK 赛事
 */
import { SimulateTemplate } from "@/templates";

export default function SimulatePage() {
  return <SimulateTemplate />;
}
