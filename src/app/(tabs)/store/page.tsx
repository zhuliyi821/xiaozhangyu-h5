/**
 * 🏪 门店页
 *
 * 使用 StoreTemplate 模板渲染
 * 可通过 config props 自定义轮播图、商品列表、门店分类和附近门店
 */
import { StoreTemplate } from "@/templates";

export default function StorePage() {
  return <StoreTemplate />;
}
