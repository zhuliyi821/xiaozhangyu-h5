/**
 * 👤 我的页
 *
 * 使用 ProfileTemplate 模板渲染
 * 可通过 config props 自定义资产、菜单项、登录状态
 */
import { ProfileTemplate } from "@/templates";

export default function ProfilePage() {
  return <ProfileTemplate />;
}
